按行排和按列排区别就是先填满行还是先填满列
但是 [ , ] 中的顺序永远都是 [ 行索引, 列索引 ]
像 A[0...x-1, 0...y-1] 那么总共行数 x，列数 y（注意题目从 0 开始的吗）
    那么例如行优先的话就是先填满同一行的所有列，A[0][0],A[0][1]...A[0][y-1]
    列优先的话就是 A[0][0],A[1][0],A[2][0]...A[x-1][0]

若对 n 阶对称矩阵 A 以行序为主序方式将其下三角的元素（包括主对角线上的所有元素）依次存放于一维数组B[1...(n(n+1)/2)]中，则在B中确认 aij(1<=i<=j<=n) 的位置 k =
    j*(j-1)/2 + i
分析：
    首先 B 中只会出现下三角，即满足 行号大于等于列号 的元素
    但是题目给的 i<=j 所以得做矩阵转换：aji
    (0+j-1)*j/2 + i

数组：相同“数”据类型的元素按一定顺序排列的集合，将有限个类型相同的变量用一个名字命名，然后用编号区分的变量集合
    所以数组元素的数据类型一定都是相同的
    数组在维数和边界确定后，其元素个数已经确定，不能插入和删除运算

# 存储地址
多维例如三维最大分别有 s1, s2, s3 的空间
那么 a[x][y][z] 的地址为
    x*s2*s3 + y*s3 + z
    直接干满（

像这种内存地址都是该元素的首地址，所以只需要计算和头之间差了多少元素->计算偏移量，不需要将自身的内存空间算入地址

# 三角矩阵
是指在主对焦及其一次有非零元素，另一侧元素全为0的矩阵
