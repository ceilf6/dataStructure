嵌套

表头：广义表的第一个元素
表尾：除表头外的剩余子表，表尾一定是表而不是元素

深度：嵌套最多的 () 的对数, 递归序列化走的最远的step

    ((A)(B)) 为 2

    A 为 0

    () 为 1


广义表的相等，类似于虚拟DOM比较，需要层结构相同、元素相同
    1. 两个空表是相等的
    2. 非空表非同类节点，表不相等
    3. 非空表，节点同类
        a. 节点元素值不同，表不同
        b. 节点元素值相同，递归调用cmp深入比较