# 链表
## 不带头链表
- 空
    L == NULL

## 带头单链表
- 空
    L.head->next == NULL
- “只有一个元素”
    L.head->next != NULL && L.next.next == L.head
    （就是将带头循环链表的 末尾判断从 L.head 改为 NULL）

## 带头循环链表
- "只有一个元素"：头节点的下一个元素，然后再指回头节点
    L.head->next != L.head && L.head->next->next==L.head
    前半部分通过断言确保安全性、防止后面指针悬空

## 带头双链表
- 空：头节点的前后指针都指向自己
    L.head->next == L.head && L.head->prior == L.head

## 循环链表
- 空：只要是循环链表判空，就是判断头指针是否指向自己
    L.head->next == L.head
- 是否到末尾
    p->next == L.head

## 不循环链表
- 是否到末尾
    p->next == NULL

## 判断是否循环
检查遍历过程中是否再次遇到头节点

## 判断是否存在环
快慢双指针
```
while()
    slow = slow->next;
    fast = fast->next->next;
如果 slow == fast 了说明有
```

# 队
教材中的
- front 指向第一个有效元素
- rear 指向最后一个元素的后面的空
但是注意具体看题目要求
## 链式实现
本质是链表，
head是虚拟头节点，第一个有效元素是 head->next
- front做头
- rear做尾指针
- 只能在 rear 入队，只在 front 出队

初始化：Q.front = Q.rear = 头节点

- 空
    Q.front == Q.rear

- 长度是否为 1
    Q.front->next = Q.rear

- 出队：特别注意长度为1的链表在出队时需要让 rear = front
```
template <class ElemType>
Status LinkQueue<ElemType>::DelQueue(ElemType &e)
{
    if (!IsEmpty()) // 即 rear != front
    {
        Node<ElemType> *p = front->next;
        e = p->data;           // 取头存储到 e 地址
        front->next = p->next; // 队头指针指向新的头，也就是第二个节点
        if (rear == p)         // 如果没有第二个节点，那么rear所在的第一个点届时会被抛弃，所以得跟上front
            rear = front;
        delete p;
        return SUCCESS;
    }
    else
        return UNDER_FLOW;
}
```

- 如果删除的是最后一个元素 ！
    Q.rear = Q.front

## 顺序实现
判空都是判断
Q.front == Q.rear

### 不循环
- 满
    rear == MAXSIZE

### 循环
learn2code/4-队列/循环顺序队/queue.cpp
- 满
    (rear+1) % MAXSIZE == front
    如果不空一个的话，队空和队满都是 rear == front
    这样的话循环队列实际最多只能有 MAXSIZE -1 个元素

# 栈
一般 top 指向的都是栈顶元素
但是具体看题目定义，有时候也会指向下一个可放元素的位置

## 链式栈
因为入栈和出栈只发生在栈顶，只需要一个 top 栈顶指针即可
- 空
    S.top == NULL

## 顺序栈
```
    int top; // 栈顶
    int maxSize;     // 栈最大容量
    ElemType *elems; // 元素存储
```
- 空
    S.top == -1 (如果s是指针的话就是s->属性)

- 满
    S.top == maxSize -1

# 队列和栈
本质都是受限制的线性表
** 后面进的就是尾巴 **
- 队只允许在表尾插入元素、在表头删除元素
- 栈只允许在表尾插入和删除元素（这里的尾巴是线性表的尾，也就是top指针）